# Frontend configuration for AI-Editor UI
# Copy this file to ".env" in the same folder if needed.

# Base URL for the FastAPI backend.
# If you run uvicorn directly on port 10000 (as in Dockerfile), this is correct:
VITE_API_BASE_URL="http://localhost:10000"

