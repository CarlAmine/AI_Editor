export function EmptyState() {
  return (
    <div className="workspace-empty">
      <h2>Start a new edit session</h2>
      <p>
        Paste a reference video URL to analyze its pacing and structure, then provide replacement
        clips, overlay text, and output preferences. The assistant will guide you through each step.
      </p>
    </div>
  );
}
